# 🎯 AI-генератор идей для digital-продуктов

Telegram-бот для генерации идей приложений с использованием Cerebras LLM.

## Возможности

- 🎯 Генерация 3-5 идей приложений по нише, бюджету и рынку
- 🔧 Детальное описание фич для каждой идеи
- ⏱ Оценка сроков разработки (MVP и полная версия)
- 💰 Оценка стоимости с учётом бюджета
- 📈 План монетизации
- ⚠️ Анализ рисков

## Технологии

- Python 3.11+
- aiogram 3.x (Telegram Bot API)
- Cerebras Cloud SDK (LLM)
- Railway (хостинг)

## Быстрый старт

### 1. Клонирование

```bash
git clone <your-repo>
cd idea-generator-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

```bash
cp .env.example .env
```

Заполни `.env`:
- `BOT_TOKEN` — токен от @BotFather
- `CEREBRAS_API_KEY` — ключ от [Cerebras Cloud](https://cloud.cerebras.ai/)

### 4. Запуск

```bash
python bot.py
```

## Деплой на Railway

### Способ 1: Через GitHub

1. Создай репозиторий на GitHub и запушь код
2. Зайди на [Railway](https://railway.app/)
3. New Project → Deploy from GitHub repo
4. Выбери репозиторий
5. Добавь переменные окружения:
   - `BOT_TOKEN`
   - `CEREBRAS_API_KEY`
6. Railway автоматически задеплоит бота

### Способ 2: Через Railway CLI

```bash
# Установка CLI
npm install -g @railway/cli

# Логин
railway login

# Инициализация проекта
railway init

# Добавление переменных
railway variables set BOT_TOKEN=your_token
railway variables set CEREBRAS_API_KEY=your_key

# Деплой
railway up
```

### Конфигурация для Railway

Создай `Procfile`:
```
worker: python bot.py
```

Или `railway.json`:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python bot.py",
    "restartPolicyType": "ON_FAILURE"
  }
}
```

## Структура проекта

```
idea-generator-bot/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости
├── .env.example        # Пример переменных окружения
├── Procfile            # Для Railway
└── README.md           # Документация
```

## Команды бота

- `/start` — Главное меню
- `/help` — Справка
- `/generate` — Быстрый старт генерации идей

## Настройки

В меню настроек можно изменить:
- **Количество идей:** 3, 4 или 5
- **Формат отчёта:** Подробный или краткий

## Сценарий использования

1. Пользователь нажимает "Сгенерировать идеи"
2. Выбирает нишу (или вводит свою)
3. Выбирает бюджет
4. Выбирает целевой рынок
5. Подтверждает и получает структурированный отчёт

## Модель LLM

Бот использует модель `llama-3.3-70b` через Cerebras API. Для изменения модели отредактируй константу `CEREBRAS_MODEL` в `bot.py`.

## Лицензия

MIT

## Автор

Создано с помощью Claude AI
